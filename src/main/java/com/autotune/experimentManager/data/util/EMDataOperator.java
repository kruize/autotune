package com.autotune.experimentManager.data.util;

import com.autotune.experimentManager.data.iteration.EMMetricData;

import java.util.ArrayList;

public class EMDataOperator {
    public static ArrayList<EMMetricData> calculateSummaryResults(ArrayList<EMMetricData> warmupResults, ArrayList<EMMetricData> measurementResults) {
        return new ArrayList<EMMetricData>();
    }
}
