package com.autotune.experimentManager.data.util;

import com.autotune.experimentManager.data.input.metrics.EMMetricResult;
import com.autotune.experimentManager.data.iteration.EMMetricData;

import java.util.ArrayList;

public class EMDataOperator {
    public static EMMetricResult calculateSummaryResults(ArrayList<EMMetricResult> warmupResults, ArrayList<EMMetricResult> measurementResults) {
        return new EMMetricResult();
    }
}
