apiVersion: "recommender.com/v1"
kind: "Autotune"
metadata:
 name: "no-slo-value"
spec:
  slo:
  mode: "show"
  selector:
    matchLabel: "app.kubernetes.io/name"
    matchLabelValue: "petclinic-deployment"
    matchRoute: ""
    matchURI: ""
    matchService: "https"
